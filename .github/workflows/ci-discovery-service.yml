name: discovery-service CI 스크립트

on:
  pull_request:
    branches:
      - main
      - closetBuddy-MSA-dev
      - rksdu/217
    paths:
      - "discovery-service/**"
      - "common-event/**"

  push:
    branches:
      - main
      - closetBuddy-MSA-dev
      - rksdu/217
    paths:
      - "discovery-service/**"
      - "common-event/**"

jobs:
  execute-test:
    uses: ./.github/workflows/ci-workflow.yml
    secrets: inherit
    with:
      service-name: discovery-service
