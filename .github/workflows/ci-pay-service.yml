name: pay-service CI 스크립트

on:
  pull_request:
    branches: [ main, closetBuddy-MSA-dev ]
    paths:
      - "pay-service/**"
      - "common-event/**"
jobs:
  execute-test:
    uses: ./.github/workflows/ci-workflow.yml
    secrets: inherit
    with:
      service-name: pay-service