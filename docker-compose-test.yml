name: closet-buddy

services:

  mysql:
    image: mysql:8.0
    container_name: closet-buddy-mysql
    ports:
      - "3308:3306"
    environment:
      MYSQL_ALLOW_EMPTY_PASSWORD: true
      MYSQL_DATABASE: closetBuddy
      MYSQL_USER: user
      MYSQL_PASSWORD: closet_Buddy1211
      TZ: Asia/Seoul
